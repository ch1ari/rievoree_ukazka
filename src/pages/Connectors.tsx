import { PageHeader } from "@/components/PageHeader"

export function Connectors() {
  return (
    <PageHeader
      title="Connectors"
      description="Google Drive auto-ingest: watch a folder, pull new files into the ETL queue. Resumable page tokens, idempotent claims. Phase 5."
      phase="Phase 5"
    />
  )
}
